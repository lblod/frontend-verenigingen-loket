{{page-title "Verenigingen"}}

<AuApp>
  <Header />

  {{#if this.session.isAuthenticated}}
    {{#if this.currentSession.currentAssociation}}
      <Shared::BreadCrumb />
    {{/if}}
    <AuMainContainer class="au-u-flex" as |m|>
      <m.content @scroll={{true}}>
        <AuBodyContainer>
          {{outlet}}
        </AuBodyContainer>
      </m.content>
    </AuMainContainer>
  {{else}}
    {{outlet}}
  {{/if}}
</AuApp>
<AuModalContainer />
<AuToaster />